{
  "name": "budget-tracking",
  "version": "1.0.0",
  "description": "Budget Tracker",
  "default": "index.html",
  "homepage": "https://DavidAdewale.github.io/budget-app",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html --dist-dir ./dist",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "clean": "rm -rf dist"
  },
  "author": "David Adewale",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "parcel": "^2.9.0"
  },
  "dependencies": {
    "core-js": "^3.30.2",
    "date-fns": "^2.30.0",
    "fractional": "^1.0.0",
    "regenerator-runtime": "^0.13.11"
  }
}
